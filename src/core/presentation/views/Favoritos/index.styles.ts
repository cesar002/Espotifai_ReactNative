import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    textTitulo: {
        fontSize:30,
        color: 'white',
        fontWeight: 'bold',
        marginVertical: 20,
        marginHorizontal: 20,
    },
});

export default styles;